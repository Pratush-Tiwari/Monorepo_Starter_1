{
  "name": "template-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.10.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "rm -rf node_modules .turbo && find . -name 'node_modules' -type d -prune -exec rm -rf '{}' +"
  },
  "devDependencies": {
    "turbo": "^2.1.3",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@t3-oss/env-core": "^0.13.8",
    "zod": "^3.23.8"
  }
}