{
  "name": "@template/db",
  "version": "0.0.0",
  "private": true,
  "description": "Prisma database package",
  "scripts": {
    "prisma:generate": "prisma generate",
    "build": "pnpm run prisma:generate",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "prisma": "^5.17.0",
    "@template/config": "workspace:*"
  }
}